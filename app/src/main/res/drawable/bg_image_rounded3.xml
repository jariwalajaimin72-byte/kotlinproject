<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- soft nude background behind image -->
    <solid android:color="@color/creamNude"/>

    <!-- rounded corners -->
    <corners android:radius="14dp"/>

    <!-- optional thin border (very premium look) -->
    <stroke
        android:width="1dp"
        android:color="#F1DADA"/>

</shape>